# This workflow is disabled — GitHub Pages is set to build directly from the main branch.
# GitHub's built-in Jekyll builder handles deployment automatically.
# To re-enable custom Actions deployment, change Pages source back to "GitHub Actions"
# and uncomment the workflow below.

# name: Deploy VBS Site to GitHub Pages
# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:
# permissions:
#   contents: read
#   pages: write
#   id-token: write
# concurrency:
#   group: "pages"
#   cancel-in-progress: false
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: ruby/setup-ruby@v1
#         with:
#           ruby-version: "3.3"
#           bundler-cache: true
#       - id: pages
#         uses: actions/configure-pages@v6
#       - run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
#         env:
#           JEKYLL_ENV: production
#       - uses: actions/upload-pages-artifact@v5
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - id: deployment
#         uses: actions/deploy-pages@v5
